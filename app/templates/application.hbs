<div class="container">
    <div class="row menu">
        {{#link-to 'index'}}
        <div class="two columns">
            <em>Demeter</em>
        </div>
        {{/link-to}}
        <div class="links nine columns">
            {{#link-to 'devices'}}
            Appareils
            {{/link-to}}
        </div>
    </div>

    <div class="body">
        {{outlet}}
    </div>
</div>
